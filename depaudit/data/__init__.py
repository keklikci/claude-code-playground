"""Static reference data bundled with depaudit.

Pure, offline data tables used by checks — no I/O, no runtime dependencies. Kept in its
own package so the tables can be imported and unit-tested independently of the checks that
consume them.
"""
